msg: >
  The conditional check '(hostvars[inventory_hostname].phase_map['STOP'] |
  selectattr('name','in', no_dep_components) | list | length) > 0' failed. The
  error was: error while evaluating conditional
  ((hostvars[inventory_hostname].phase_map['STOP'] | selectattr('name','in',
  no_dep_components) | list | length) > 0): 'no_dep_components' is undefined.
  'no_dep_components' is undefined


  The error appears to be in '/runner/project/playbook.yml': line 140, column 7,
  but may

  be elsewhere in the file depending on the exact syntax problem.


  The offending line appears to be:


      - name: Stop (Linux) — parallel dependency-free components
        ^ here
_ansible_no_log: false 
