I think the metrics-catalog.yml is only used to generate a separate report I believe is use to generate "additions.ndjson" which
is use to generate a local report I don think we need to use it to forward to splunk except all the other json files in catalog

I agree with your interpretation.
metrics-catalog.yml is a local reporting workflow, not part of the core catalog production flow and not the best component to forward data to Splunk.
It currently:
Clones the catalog repository with full history.

Generates:
additions.ndjson
scans.ndjson
all-events.ndjson

Uses those generated files to build:
REPORT.md
DASHBOARD.html

Commits selected report outputs back into the catalog repository.

Notably, scans.ndjson and all-events.ndjson are explicitly excluded from Git because they become too large. Therefore, this pipeline is designed primarily for regenerated local reports, not durable event forwarding.
Revised recommendation
Splunk should not consume:
metrics/events/additions.ndjson
metrics/events/scans.ndjson
metrics/events/all-events.ndjson
metrics/REPORT.md
metrics/DASHBOARD.html
Splunk should instead consume changes to the authoritative catalog JSON files, such as:
vendor-catalog/containers/*.json
vendor-catalog/charts/*.json
vendor-catalog/library/*.json
vendor-catalog/packages/*.json
The exact paths should be confirmed against the catalog repository.
Important distinction
Sending the complete JSON files to Splunk after every update is possible, but it may cause:
Duplicate records
Large payloads
Difficulty distinguishing new entries from modified entries
Incorrect historical counts
Repeated vulnerability data
Difficulty detecting removals
A better model is:
Catalog JSON change
        │
        ▼
Compare previous commit with current commit
        │
        ▼
Generate only changed artifact events
        │
        ▼
Send changed events to Splunk HEC
For example, when a container JSON changes, the exporter should classify each difference as:
artifact_added
release_added
scan_updated
status_changed
artifact_removed
artifact_blacked_out
suppression_added
suppression_expired
whitelist_expired
whitelist_renewed
The existing extract_from_catalog_commits.py can be used as a reference for understanding the catalog schemas and comparison logic, but the Splunk implementation should not depend on the generated NDJSON report files.
Recommended catalog exporter
The exporter should run from RECORE.third-party-catalog when authoritative files change on main:
on:
  push:
    branches: [main]
    paths:
      - "vendor-catalog/containers/**"
      - "vendor-catalog/charts/**"
      - "vendor-catalog/library/**"
      - "vendor-catalog/packages/**"
It should compare:
github.event.before → github.sha
This allows it to send only the changes introduced by that catalog update.
A scheduled reconciliation workflow can remain as a recovery mechanism, but normal Splunk delivery should be event-driven.
What remains outside the catalog
The catalog can supply authoritative final outcomes, including:
Image published
Image active/inactive/suppressed
New image or release
Scan severity information
Scan-gate status
Artifactory destination
Blackout or removal, when represented in the catalog
Expiration or renewal, if those fields are added
But it cannot reliably supply:
Whitelist request initiated
Validation failures before catalog creation
Ingestion attempt started
Vulnerability scan initiated
Workflow cancellation
Runner or infrastructure failure
Request rejected before reaching the catalog
Detailed pipeline duration
Those still require request and GitHub Actions events.
Corrected architecture
IssueOps / request workflow
    └── request initiated, validation, early rejection

GitHub Actions collector
    └── workflow started, completed, failed, cancelled, duration

Scan instrumentation
    └── scan initiated, completed, failed

Authoritative catalog JSON changes
    └── published, gated, suppressed, removed, new release, final scan state
                         │
                         ▼
                     Splunk HEC
So the corrected conclusion is:
Do not use metrics-catalog.yml or its generated NDJSON files as the Splunk source. Read changes directly from the authoritative catalog JSON files. Reuse only the extractor’s schema knowledge and diffing ideas where helpful.

This also avoids coupling Splunk ingestion to a separate reporting workflow that runs daily and regenerates historical metrics.
========================================
No—the recurring container scan does not appear to delete an already-published image from Artifactory when it becomes inactive. It changes the catalog status to inactive and updates scan/security fields. Any docker image rm commands are runner cleanup, not Artifactory deletion. See [image-recurring-scan.yml (line 657)](/Users/gilbertomendez/RECORE.third-party/.github/workflows/image-recurring-scan.yml:657).
The existing release entry is updated in place: .status and .scanned_date are overwritten during each scan rather than creating a new dated entry. See [image-recurring-scan.yml (line 1223)](/Users/gilbertomendez/RECORE.third-party/.github/workflows/image-recurring-scan.yml:1223).
Recommended approach:
Keep one catalog entry representing the image’s current state.
Emit a separate append-only Splunk event whenever status changes, containing previous_status, new_status, changed_at, and reason.
Do not create another catalog release entry for the same image/version; that would duplicate inventory and distort whitelist totals.

==============
It is technically possible, but medium-to-high complexity.
An external forwarder could periodically snapshot the catalog JSON files, compare each artifact/version with its previous snapshot, and detect:
New entries
Status transitions such as active → inactive
Changed scanned_date or vulnerability counts
Removals
It would require persistent state/checkpoints, deterministic IDs, support for multiple catalog JSON schemas, duplicate prevention, and handling simultaneous catalog updates.
However, catalog-only forwarding cannot satisfy all telemetry requirements because the JSON files do not contain every workflow event, request timestamp, approval decision, failure reason, or scan-start event. It is viable for current-state and catalog-change reporting, but not as the sole source for complete operational and audit telemetry.
Full snapshots become progressively slower and more expensive as the catalog grows: every run must download, parse, normalize, compare, and checkpoint every entry—even when only one image changed.
You can reduce the impact by storing only a compact hash/state record per artifact_id and processing only catalog files changed since the last commit. However, this effectively becomes a stateful catalog-diff system with checkpointing, merge handling, schema normalization, deduplication, and recovery logic.
So full-catalog snapshots are reasonable as an occasional reconciliation safety net, but they are not a good primary real-time forwarding mechanism. This scalability and operational complexity is one reason write-time telemetry remains the stronger long-term design.
======================= Metrics-catalgog.yml ===================
Confirmed. Here's what metrics-catalog.yml actually does:

It's a read-only reporting pipeline — it never touches vendor-catalog/. It runs in a separate job that checks out both repos, but only reads from vendor-catalog/*.json and only writes to metrics/ (NDJSON, REPORT.md, DASHBOARD.html, README.md). It has no code path that mutates the actual catalog data files — those are written elsewhere, by the lifecycle/recurring-scan workflows.

It does capture history, not just a current-state snapshot — and it does this cleverly, from git itself rather than any live event log:

extract_from_catalog_commits.py checks out the catalog repo with fetch-depth: 0 (full history) and walks, per vendor JSON file, every commit that ever touched it (get_file_commits).
For each commit in order, it pulls that file's content at that commit (git show <hash>:<path>) and diffs it against the previous commit's snapshot of the same file (prev_state).
From that diff it derives discrete events:
software_added / release_added — a brand-new software entry or tag/version appears
scan_updated — an existing tag's scanned_date or policy_gate changed (i.e., a rescan flipped it active/inactive/suppressed)
These events get appended into additions.ndjson and scans.ndjson (and a merged all-events.ndjson), each carrying the commit hash, timestamp, and subject line — so it's effectively a derived changelog/audit trail of the catalog's evolution, reconstructed by replaying git history, not a live-captured stream.
generate_report.py and generate_dashboard.py then just consume those NDJSON files to build the human-facing REPORT.md and DASHBOARD.html.
One caveat worth knowing: this is a full re-walk every run (no incremental state) — since it processes the entire commit history of every catalog file from scratch each time (daily cron + on push to catalog main), it's expensive but self-healing; there's no risk of drift between the NDJSON and the actual catalog history since it's regenerated wholesale each time. The two largest event logs (all-events.ndjson, scans.ndjson) are deliberately excluded from the commit (.gitignore'd, only used in-run) because they'd blow past GitHub's 100MB file limit — only the small additions.ndjson plus the generated REPORT.md/DASHBOARD.html/README.md get committed back.
